{

	"name": "mern-app",
	"version": "1.0.0",
	"scripts": {
	  "client": "pnpm --filter client dev",    
	  "server": "pnpm --filter server dev",    
	  "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",  
	  "build": "pnpm --filter client build",  
	  "start": "pnpm run build && pnpm --filter server start" 
	},
	"dependencies": {
		"concurrently": "^9.0.1"
	}
}
